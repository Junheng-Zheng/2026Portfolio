import P from "./P";

const Constraints = ({ children }) => {
  return (
    <div className="flex flex-col gap-5 w-[400px] bg-white rounded-xl  h-fit p-[20px] ">
      {children}
    </div>
  );
};

export default Constraints;
